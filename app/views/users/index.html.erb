<div class="container">
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <h1 class="text-center white-bold">DevMatch Community</h1>
      <p class="text-center white-bold">Welcome to the DevMatch community. 
      Users must fill out a profile in order to be visible in the community.<br> 
      Click on a users name or picture to learn more!</p>
      <ul class="list-unstyled">
        <% @users.each do |user| %>
          <% if user.profile %>
            <li>
              <div class="well row <%= cycle('gray-bg white-bold', 'gray-bg2 white-bold') %>">
                <div class="col-sm-4 text-center">
                  <% if user.profile.avatar %>
                    <%= link_to user do %>
                      <%= image_tag user.profile.avatar.url, class: 'user-index-avatar' %>
                    <% end %>
                  <% end %>
                </div>
                <div>
                  <%= link_to user do %>
                    <h2 class="text-center"><%= user.profile.first_name %> <%= user.profile.last_name %></h2>
                  <% end %>
                  <p class="text-center">
                    <%= user.profile.job_title %>
                    <% if user.plan_id == 2 %>
                      <span class="label label-danger">PRO</span>
                    <% end %>
                  </p>
                </div>
              </div>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</div>